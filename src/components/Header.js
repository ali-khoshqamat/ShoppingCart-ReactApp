import Navigation from "./Navigation";

const Header = () => {
  return (
    <header className="sticky top-0 right-0 z-50 w-full pb-2 text-center">
      <Navigation />
    </header>
  );
};

export default Header;
